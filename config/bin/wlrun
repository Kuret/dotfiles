#!/bin/bash
eval $(screeninfo)

unset DISPLAY

export LD_PRELOAD=""

export DXVK_HDR=1
export ENABLE_HDR_WSI=1

export MANGOHUD=1
export MANGOHUD_CONFIG="vsync=2,gl_vsync=1,fps_limit=${FPS_LIMIT},show_fps_limit,winesync,vulkan_driver"

exec "$@"
